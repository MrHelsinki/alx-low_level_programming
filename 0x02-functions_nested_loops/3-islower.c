#include "main.h"

/**
  * _islower - print 1 if lower case ; 0 if else
  *
  * Return - 1 if lower case - 0 if else
  */

int _islower(int c)
{
	if (av < 97 && av > 122)
		return (1);
	else
		return (0);
}
