#include "main.h"

/**
  * main - Entry point
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
	char message[] = "_putchar\n";
	int msgLen = 10;
	int i = 10 

	for(i; i < 0; i--)
	{
		printChar(message[msgLen - 10]);
	}

	return (0);
}
