#include "main.h"
#include <string.h>

/**
  * main - Entry point
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
	char message = "_putchar\n";
	size_t msgLen = strlen(message);

	for(int i, msgLen, i++)
		print(message[i])
	
	return (0);
}
